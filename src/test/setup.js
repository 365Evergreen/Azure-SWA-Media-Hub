import '@testing-library/jest-dom'
import React from 'react'

// Make React available globally so JSX tests don't need explicit imports
// when using the classic JSX runtime in test environments
globalThis.React = React
